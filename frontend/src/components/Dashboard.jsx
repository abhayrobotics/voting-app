import { useRef } from "react"


const Dashboard = ({ data }) => {
  const name = useRef()
  const age = useRef()
  const party = useRef()
  return (
    <div>
      {/* input */}
      


    </div>
  )
}

export default Dashboard